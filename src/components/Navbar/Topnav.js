import React from "react";
import { NavTop } from "./navbarStyles";
const Topnav = () => {
  return (
    <NavTop>
      <button>Back</button>
      <button>Save</button>
    </NavTop>
  );
};

export default Topnav;
